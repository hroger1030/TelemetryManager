namespace TelemetryManager
{
    /// <summary>
    /// These values corespond to the specific names of the appenders in the log4net.config file.
    /// </summary>
    public enum Appender
    {
        ConsoleAppender,
        ManagedColoredConsoleAppender,
        LogglyAppender,
        FileAppender,
    }
}
